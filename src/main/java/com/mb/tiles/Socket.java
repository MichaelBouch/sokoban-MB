package com.mb.tiles;

import java.awt.image.BufferedImage;

// Socket class inherits Floor class fields and methods
public class Socket extends Floor {

    public Socket(BufferedImage texture) {
        super(texture);
    }
}
